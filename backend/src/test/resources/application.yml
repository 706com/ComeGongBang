app:
  redirect-url: http://localhost:3000

server:
  port: 8090

spring:
  data:
    redis:
      port: 6379
      host: localhost
    elasticsearch:
      url: test
      host: tests
      port: 60010
      repositories:
        enabled: true

  jwt:
    secret: qwertyuiopasdfghjklzxcvbnm

  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://127.0.0.1:3306/synergy_test
    username: root
    password: 123qwe

  jpa:
    defer-datasource-initialization: true
    database-platform: org.hibernate.dialect.MariaDBDialect
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true

  sql:
    init:
      mode: always

  mail:
    host: smtp.gmail.com  #SMTP sever host  : fix address
    port: 587 #SMTP server port : fix value
    username: test
    password: test
    properties:
      mail:
        smtp:
          starttls:
            enable: true  # start TLS
            required: true
          auth: true
          connectiontimeout: 15000
          timeout: 15000
          writetimeout: 15000

  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: test
            redirect-uri: test
            authorization-grant-type: authorization_code
            client-authentication-method: client_secret_post
            client-name: Kakao
            scope:
              - profile_nickname
              - account_email
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id
  web:
    resources:
      add-mappings: false

aes:
  secret: AES
  algorithm: qwertyuiopasdfghjklzxcvbnm

imp:
  api: test
  secret : test

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace  #show '?' in sql in console


queue:
  enabled: true
  max-size-active: 50
  fixed-delay : 2000
  necessary: false
